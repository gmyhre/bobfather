<%= form_tag "/bobfathers" do |f| %>
  <% if @bobfather and @bobfather.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bobfather.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @bobfather.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= label_tag :bobchild %><br />
    <%= select_tag :u1, options_from_collection_for_select(User.all, :id, :name) %>
  </div>
  <div class="field">
    <%= label_tag :bobfather %><br />
    <%= select_tag :u2, options_from_collection_for_select(User.all, :id, :name) %>
  </div>
  <div class="actions">
    <%= submit_tag "submit" %>
  </div>
<% end %>
